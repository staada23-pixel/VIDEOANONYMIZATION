colors:
  active:   [255, 0, 0]       # BGR — červená
  low:      [255, 200, 0]     # BGR — žlutá
  kcf_ok:   [255, 140, 0]     # BGR — oranžová
  waiting:  [140, 40, 140]    # BGR — fialová

box_thickness: 2              # null = automaticky podle rozlišení
font_scale: 0.5
show_id: true
show_confidence: true
show_psr: true
show_lost_frames: true
show_info_bar: true           # řádek nahoře s Frame/Det/KCF/CMC info
